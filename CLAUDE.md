# A股短线模型工作台 - 项目状态总结

## 项目概述

A股短线交易辅助系统，基于情绪周期和板块轮动的量化选股和决策支持。

**技术栈**：Node.js + WebSocket，前端单页应用
**数据源**：东方财富、同花顺涨停池
**核心功能**：实时选股、买卖决策、复盘分析

## 最新修复（2026-07-11）

### 问题描述
- ❌ 主线错误判定为"军工信息化"（实际应为"存储芯片/AI算力"）
- ❌ bestPicks错误显示海兰信（军工票）
- ❌ 候选池混入非主线票或为空

### 根本原因
1. 题材聚类把军工和AI算力合并成一个簇（错误吞并）
2. 主线选择优先级不合理（共振+低连板压过高连板）
3. focusConcept定义多处不一致
4. 代码初始化顺序问题（topicBoard引用错误）

### 核心修复（通用逻辑）

#### 1. 题材聚类隔离 (clusterHotConcepts)
```javascript
// 添加互斥规则，防止跨体系合并
const exclusiveGroups = [
  军工体系: ["军工信息化", "军工", "军民融合", "商业航天", ...],
  AI算力体系: ["存储芯片", "CPO", "先进封装", "算力租赁", ...],
];
```
**普适性**：可扩展到任意体系（新能源、生物医药等）

#### 2. 主线选择优化 (buildTopicBoard)
```
新优先级：
高连板(≥7板) > sustained(结构完整) > 中连板(≥4板) > 共振+连板
```
**原理**：
- 绝对强度优先（≥7板直接选，不看共振）
- 防止单一维度误导（6板+共振不应压过9板）
- 多维度综合判断（连板+共振+梯队结构）

**验证场景**：
- 存储9板 vs 军工6板+共振 → 选存储 ✅
- 光伏8板 vs 人形5板+共振 → 选光伏 ✅（未来场景）

#### 3. 焦点概念统一
- scoreCandidate第一轮：用hotConcepts[0]临时打分（避免初始化错误）
- topicBoard创建后：基于mainLine二次精确过滤
- 全链路统一使用真实主线

#### 4. 初始化顺序修复
- 解决"Cannot access 'topicBoard' before initialization"错误
- 分两阶段过滤，避免引用未定义变量

### 验收结果
- ✅ 周期：混沌
- ✅ 主线：存储芯片 (limitCount: 10)
- ✅ hotConcepts：存储芯片和军工信息化已分离
- ✅ 候选池：深科技（存储芯片中军）
- ✅ bestPicks：深科技（不再是海兰信）

### 相关提交
- `d201519` - 聚类隔离+主线选择+焦点统一
- `b3b2894` - 优化建议文档
- `fcb08e7` - 优先级调整+初始化顺序修复

## 核心模块说明

### 1. server.js (核心服务)
- `clusterHotConcepts()` - 题材聚类（含互斥规则）
- `buildTopicBoard()` - 主线判断（多维度综合）
- `scoreCandidate()` - 个股打分
- `buildBestPicks()` - 明日最优解生成

### 2. 交易引擎
- `sell-engine.js` - 卖出时间轴闸门
- `preplan.js` - 盘前预案
- `journal.js` - 交易日志

### 3. 数据源
- `eastmoney-fetcher.js` - 东方财富数据
- `news-fetcher.js` - 消息面抓取

## 关键配置参数

### 主线选择阈值
```javascript
veryHighLimit: 7    // 高连板阈值
mediumLimit: 4      // 中连板阈值
resonanceMinLimit: 2  // 共振最低连板要求
```

### 互斥体系（可扩展）
当前定义：军工体系、AI算力体系
未来可添加：新能源体系、生物医药体系、消费体系

## 待优化项（优先级）

### P1（下一步）
1. **配置化** - 将互斥规则和阈值移到配置文件
2. **测试覆盖** - 添加单元测试防止回归
3. **监控告警** - 主线判断异常检测

### P2（中长期）
4. 主线切换平滑性（避免频繁切换）
5. 候选池数量控制（3核心+5中军+7备选）
6. bestPicks策略细化（执行级+观察级）
7. 混沌期策略专项优化

## 重要提醒

### 修复的是通用逻辑，不是特例
- ❌ 不是：让这次选对存储芯片
- ✅ 而是：修复选股逻辑，适用于任何题材组合
- 原则：多维度综合判断，不依赖具体题材名称

### 不要动的核心逻辑
- 四道闸结构（hardGate/softGate/setup/rejects）
- 周期门槛（66/72/78/86/99）
- 打分体系基础框架

### 如何扩展互斥规则
当出现新的题材合并问题时：
1. 在exclusiveGroups添加新体系
2. 定义该体系的核心概念列表
3. 重启服务器生效

## 启动命令

```bash
# 开发环境
node server.js

# 生成静态站点
node build-static.mjs

# 盘后归档
node archiver.js

# 测试
node test-*.js
```

## 文档索引

- [修复记录](docs/fix-mainline-issue-2026-07-11.md) - 详细修复过程
- [优先级修复](docs/fix-mainline-priority-2026-07-11.md) - 优先级调整说明
- [逻辑验证](docs/logic-universality-verification.md) - 普适性验证
- [优化建议](docs/optimization-suggestions.md) - 后续优化路线图
- [集成说明](INTEGRATION.md) - 模块集成文档
- [工作进展](工作进展与交接.md) - 历史进展记录

## 当前状态

- ✅ 服务运行正常：http://localhost:5173
- ✅ 主线判断准确：存储芯片（AI算力体系）
- ✅ 聚类逻辑修复：军工和AI算力正确分离
- ✅ 代码已推送：GitHub master分支

---

**最后更新**：2026-07-11
**Git最新提交**：fcb08e7
